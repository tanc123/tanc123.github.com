// JavaScript Document
(function (angular) {
    var indexOf = [].indexOf || function (item) {
            for (var i = 0, l = this.length; i < l; i++) {
                if (i in this && this[i] === item) {
                    return i
                }
            }
            return -1
        };
    angular.module("pickadate.utils", []).factory("pickadateUtils", ["dateFilter", function (dateFilter) {
        return {
            isDate: function (obj) {
                return Object.prototype.toString.call(obj) === "[object Date]"
            }, stringToDate: function (dateString) {
                if (this.isDate(dateString)) {
                    return new Date(dateString)
                }
                dateString = dateString.split("-");
                dateString[2] = dateString[2] < 10 ? "0" + dateString[2] : dateString[2];
                dateString = dateString.join("-");
                console.log(dateString);
                var dateParts = dateString.split("-"), year = dateParts[0], month = dateParts[1], day = dateParts[2];
                return new Date(year, month - 1, day, 3)
            }, dateRange: function (first, last, initial, format) {
                var date, i, _i, dates = [];
                if (!format) {
                    format = "yyyy-MM-dd"
                }
                for (i = _i = first; first <= last ? _i < last : _i > last; i = first <= last ? ++_i : --_i) {
                    date = this.stringToDate(initial);
                    date.setDate(date.getDate() + i);
                    dates.push(dateFilter(date, format))
                }
                return dates
            }
        }
    }]);
    angular.module("pickadate", ["pickadate.utils"]).directive("pickadate", ["$locale", "pickadateUtils", "dateFilter", function ($locale, dateUtils, dateFilter) {
        return {
            require: "ngModel",
            scope: {date: "=ngModel", minDate: "@min", maxDate: "=", disabledDates: "="},
            template: '<div class="pickadate" min="2015">' + '<div class="pickadate-header">' + '<div class="pickadate-controls">' + '<button class="pickadate-prev" ng-click="changeMonth(-1)" ng-show="allowPrevMonth">&lt;</button>' + '<button class="pickadate-next" ng-click="changeMonth(1)" ng-show="allowNextMonth">&gt;</button>' + "</div>" + '<h3 class="pickadate-centered-heading">' + '{{currentDate | date:"yyyy"}}年{{currentDate | date:"MM"}}月' + "</h3>" + "</div>" + '</div><div class="pickadate-body">' + '<div class="pickadate-main">' + '<ul class="pickadate-cell dateDates">' + '<li class="pickadate-head" ng-repeat="dayName in dayNames">' + "{{dayName}}" + "</li>" + "</ul>" + '<ul class="pickadate-cell">' + '<li ng-repeat="d in dates" ng-click="setDate(d)" class="{{d.className}}" ng-class="{\'pickadate-active\': date == d.date}">' + '{{d.date | date:"d"}}' + "</li>" + "</ul>" + "</div>" + "</div>" + "</div>",
            link: function (scope, element, attrs, ngModel) {
                var maxDate = scope.maxDate && dateUtils.stringToDate(scope.maxDate), minDate = scope.minDate && dateUtils.stringToDate(scope.minDate), disabledDates = scope.disabledDates || [], currentDate = new Date();
                console.log(minDate);
                scope.dayNames = ["日", "一", "二", "三", "四", "五", "六"];
                scope.currentDate = currentDate;
                scope.render = function (initialDate) {
                    initialDate = new Date(initialDate.getFullYear(), initialDate.getMonth(), 1, 3);
                    var currentMonth = initialDate.getMonth() + 1, dayCount = new Date(initialDate.getFullYear(), initialDate.getMonth() + 1, 0, 3).getDate(), prevDates = dateUtils.dateRange(-initialDate.getDay(), 0, initialDate), currentMonthDates = dateUtils.dateRange(0, dayCount, initialDate), lastDate = dateUtils.stringToDate(currentMonthDates[currentMonthDates.length - 1]), nextMonthDates = dateUtils.dateRange(1, 7 - lastDate.getDay(), lastDate), allDates = prevDates.concat(currentMonthDates, nextMonthDates), dates = [], today = dateFilter(new Date(), "yyyy-MM-dd");
                    if (allDates.length / 7 < 6) {
                        allDates = allDates.concat(dateUtils.dateRange(1, 8, allDates[allDates.length - 1]))
                    }
                    var nextMonthInitialDate = new Date(initialDate);
                    nextMonthInitialDate.setMonth(currentMonth);
                    scope.allowPrevMonth = !minDate || initialDate > minDate;
                    scope.allowNextMonth = !maxDate || nextMonthInitialDate < maxDate;
                    for (var i = 0; i < allDates.length; i++) {
                        var className = "", date = allDates[i];
                        if (date < scope.minDate || date > scope.maxDate || dateFilter(date, "M") !== currentMonth.toString()) {
                            className = "pickadate-disabled"
                        } else {
                            if (indexOf.call(disabledDates, date) >= 0) {
                                className = "pickadate-disabled pickadate-unavailable"
                            } else {
                                className = "pickadate-enabled"
                            }
                        }
                        if (date === today) {
                            className += " pickadate-today"
                        }
                        dates.push({date: date, className: className})
                    }
                    scope.dates = dates
                };
                scope.setDate = function (dateObj) {
                    if (isDateDisabled(dateObj)) {
                        return
                    }
                    ngModel.$setViewValue(dateObj.date)
                };
                ngModel.$render = function () {
                    if ((date = ngModel.$modelValue) && (indexOf.call(disabledDates, date) === -1)) {
                        scope.currentDate = currentDate = dateUtils.stringToDate(date)
                    } else {
                        if (date) {
                            scope.setDate({})
                        }
                    }
                    scope.render(currentDate)
                };
                scope.changeMonth = function (offset) {
                    currentDate.setDate(1);
                    currentDate.setMonth(currentDate.getMonth() + offset);
                    scope.render(currentDate)
                };
                function isDateDisabled(dateObj) {
                    return (/pickadate-disabled/.test(dateObj.className))
                }
            }
        }
    }])
})(window.angular);
function echo(stringA, stringB) {
    var hello = "你好";
    alert("hello world")
};